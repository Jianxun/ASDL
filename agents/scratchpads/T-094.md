# Task summary (DoD + verify)
- Extend PatternAtomizePass to enforce that endpoint instance atoms resolve to declared instance atoms and to materialize per-atom instance params using patterned-parameter expansion rules (broadcast/zip with diagnostics).
- Add tests for subset endpoint bindings and parameter expansion diagnostics.
- Verify: pytest tests/unit_tests/ir/test_pattern_atomization.py -v

# Read (paths)
- agents/context/lessons.md
- agents/context/contract.md
- agents/context/tasks.yaml
- agents/context/tasks_state.yaml
- agents/context/project_status.md

# Plan
- Update task state + branch; outline subtasks.
- Add tests for endpoint subset bindings and parameter expansion diagnostics.
- Implement endpoint validation + per-atom param materialization in PatternAtomizePass.
- Run verify; update scratchpad and task state; open PR.

# Progress log
- 2026-01-13: Initialized scratchpad.

# Patch summary

# PR URL

# Verification

# Status request (Done / Blocked / In Progress)
In Progress

# Blockers / Questions

# Next steps
- Implement atomization endpoint validation and param expansion.
