schema_version: 2

current_sprint:
  - id: T-240
    title: Expand literal patterns in visualizer dump + add numeric labels
    owner: Executor
    dod: >
      Update visualizer dump generation to expand literal enum patterns in
      instance names and endpoint pin names for visualization. Keep numeric
      range patterns compact but attach per-endpoint numeric labels formatted
      as `<3>` or tuple form `<3,1>`; join multiple slices with `;` following
      `docs/specs/spec_asdl_pattern_expansion.md`. Ensure ota_nmos visualizer
      dump emits endpoints for `mn_tail.<s|b>` and `mn_in_<n>.d`.
    verify:
      - ./venv/bin/asdlc visualizer-dump examples/libs/mosbius_devices/ota_nmos/ota_nmos.asdl
    links:
      scratchpad: agents/scratchpads/T-240_visualizer_literal_expand.md
    files:
      - src/asdl/core/dump.py
      - src/asdl/core/registries.py
      - src/asdl/patterns_refactor/
  - id: T-241
    title: Consume endpoint labels + pin label policy in extension graph
    owner: Executor
    depends_on:
      - T-240
    dod: >
      Extend visualizer dump types and symbol parsing to include pin label
      policy (`label: auto|always|never`). Build GraphPayload edges with
      optional `conn_label` and emit per-pin label data for the webview. Prefer
      numeric `conn_label` over pin `label: always`.
    verify:
      - cd extensions/asdl-visualizer && npm run build
    links:
      scratchpad: agents/scratchpads/T-241_visualizer_pin_label_policy.md
    files:
      - extensions/asdl-visualizer/src/extension/types.ts
      - extensions/asdl-visualizer/src/extension/symbols.ts
      - examples/pdks/gf180mcu/asdl/gf180mcu.sym.yaml
  - id: T-242
    title: Render numeric pin labels + forced labels in webview
    owner: Executor
    depends_on:
      - T-241
    dod: >
      Render connection labels at pin handles for numeric patterns, using
      `<3>` and tuple `<3,1>` formatting and `;` joins. Honor pin `label:
      always` by rendering net/slice labels when no numeric label exists.
      Labels must respect symbol orientation and stay inside the body edge.
    verify:
      - cd extensions/asdl-visualizer && npm run build
    links:
      scratchpad: agents/scratchpads/T-242_visualizer_pin_labels.md
    files:
      - extensions/asdl-visualizer/src/webview/app.tsx
      - extensions/asdl-visualizer/src/webview/styles.css
      - extensions/asdl-visualizer/src/webview/devHarness.ts

backlog: []
