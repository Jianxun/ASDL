# Lessons

## Pending additions
- Always pull `main` before committing directly to it to avoid diverged history.
- Apply DRY by default in task slicing: require a quick reuse audit before
  coding and prefer shared helper extraction over parallel policy logic.
