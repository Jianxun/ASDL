file_info:
  top_module: prim
  doc: "Primitive with ordered ports for IR tests"

modules:
  prim:
    doc: "Test primitive module"
    ports:
      a: {dir: in, type: signal}
      b: {dir: out, type: power}
      c: {dir: in_out, type: ground}
    spice_template: |
      R{name} a b 1k

