top: top
modules:
  top:
    variables:
      known: 2k
    instances:
      R1: res r={missing}
    nets:
      $IN:
        - R1.P
      $OUT:
        - R1.N
devices:
  res:
    ports: [P, N]
    parameters:
      r: 1k
    backends:
      sim.ngspice:
        template: "{name} {ports} {params}"
