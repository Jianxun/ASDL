file_info:
  name: top
  top_module: top

imports:
  op: opamp.asdl

modules:
  top:
    ports:
      in_p: {dir: in}
      in_n: {dir: in}
      vdd: {dir: in}
      vss: {dir: in}
      out: {dir: out}
    instances:
      A1: {model: op.ota_diffpair, mappings: {in_p: in_p, in_n: in_n, vdd: vdd, vss: vss, out: out}}

